<h1>Individuos list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Plantacion</th>
            <th>Coordenadax</th>
            <th>Coordenaday</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('individuos_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.plantacion }}</td>
            <td>{{ entity.coordenadax }}</td>
            <td>{{ entity.coordenaday }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('individuos_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('individuos_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('individuos_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
