<h1>Individuos creation</h1>

<form action="{{ path('individuos_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('individuos') }}">
            Back to the list
        </a>
    </li>
</ul>
